# API Service Dependencies
# Minimal dependencies for the FastAPI REST API service

# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
slowapi>=0.1.9  # Rate limiting for FastAPI

# Database drivers (async)
asyncpg>=0.29.0  # PostgreSQL async driver
redis>=5.0.0  # Redis for caching and rate limiting
qdrant-client>=1.7.0  # Vector database client

# Data validation
pydantic>=2.5.0

# GraphQL support (optional)
strawberry-graphql[fastapi]>=0.215.0

# HTTP client (for external API calls)
aiohttp>=3.9.0

# Monitoring and observability
prometheus-client>=0.19.0
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
opentelemetry-exporter-otlp-proto-http>=1.21.0
opentelemetry-instrumentation-fastapi>=0.42b0
opentelemetry-instrumentation-aiohttp-client>=0.42b0
opentelemetry-instrumentation-redis>=0.42b0
opentelemetry-instrumentation-asyncpg>=0.42b0

# Utilities
python-dotenv>=1.0.0

# Core shared library (installed from local packages directory)
# Install with: pip install -e ../packages/trend-agent-core
# This provides:
#   - trend_agent.storage (PostgreSQL, Redis, Qdrant repositories)
#   - trend_agent.ingestion (Plugin manager)
#   - trend_agent.observability (Metrics, tracing)

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
