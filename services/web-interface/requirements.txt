# Web Interface Service Dependencies
# Django-based web UI for the Trend Intelligence Platform

# Django web framework
django>=4.2.0
django-redis>=5.4.0  # Redis cache backend for Django
psycopg2-binary>=2.9.9  # PostgreSQL adapter for Django
gunicorn>=21.2.0  # WSGI HTTP server for production

# Database drivers
asyncpg>=0.29.0  # PostgreSQL async driver (for async views)

# HTTP client (for calling the API service)
aiohttp>=3.9.0
httpx>=0.25.0

# Templates and static files
whitenoise>=6.6.0  # Serve static files in production

# Utilities
python-dotenv>=1.0.0

# Monitoring (optional)
prometheus-client>=0.19.0

# Core shared library (installed from local packages directory)
# Install with: pip install -e ../packages/trend-agent-core
# This provides:
#   - trend_agent.storage (PostgreSQL, Redis repositories)
#   - trend_agent.observability (Metrics)

# Testing
pytest>=7.4.0
pytest-django>=4.7.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Development
django-debug-toolbar>=4.2.0  # Debug toolbar (disable in production)
