# Crawler Service Dependencies
# Data collection service for the Trend Intelligence Platform

# Task scheduling
apscheduler>=3.10.4

# HTTP client for API calls
aiohttp>=3.9.0

# Data collection libraries
pytrends>=4.9.0  # Google Trends API
feedparser>=6.0.10  # RSS feed parsing
beautifulsoup4>=4.12.0  # HTML parsing
trafilatura>=1.6.0  # Web content extraction

# NLP for text processing
langdetect>=1.0.9  # Language detection

# CJK and text processing (for content normalization)
jieba>=0.42.1  # Chinese segmentation
pypinyin>=0.50.0  # Chinese romanization
pykakasi>=2.2.1  # Japanese romanization
hangul-romanize>=0.1.0  # Korean romanization
transliterate>=1.10.2  # Cyrillic romanization

# Database drivers (async)
asyncpg>=0.29.0  # PostgreSQL async driver
redis>=5.0.0  # Redis for caching

# Data validation
pydantic>=2.5.0

# Utilities
python-dotenv>=1.0.0

# Monitoring
prometheus-client>=0.19.0
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
opentelemetry-instrumentation-aiohttp-client>=0.42b0

# Core shared library (installed from local packages directory)
# Install with: pip install -e ../packages/trend-agent-core
# This provides:
#   - trend_agent.collectors (Plugin-based collectors)
#   - trend_agent.ingestion (Plugin manager)
#   - trend_agent.storage (PostgreSQL, Redis repositories)
#   - trend_agent.observability (Metrics, tracing)

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-aiohttp>=1.0.5
